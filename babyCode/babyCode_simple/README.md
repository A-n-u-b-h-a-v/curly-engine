# Baby Code Simple - Clean Landing Page

This project serves as a clean, straightforward implementation of a modern landing page using React and Tailwind CSS. It focuses on solid component structure and responsive design without the complexity of heavy animation libraries.

## 🚀 Tech Stack

- **[React](https://react.dev/)**: For building reusable UI components.
- **[Tailwind CSS](https://tailwindcss.com/)**: For rapid, clean, and responsive styling.
- **[Vite](https://vitejs.dev/)**: For instant server start and fast HMR (Hot Module Replacement).

## ✨ Features

- **Responsive Design**: Looks great on mobile, tablet, and desktop.
- **Modular Components**: Clean separation of concerns with components like:
  - `Hero`: The main landing section.
  - `Features`: Highlighting key offerings.
  - `Testimonials`: Social proof section.
  - `Navbar` & `Footer`: Standard navigation elements.
- **Performance**: Lightweight and fast-loading due to minimal dependencies.

## 🛠️ Installation & Setup

1.  **Navigate to the directory**:
    ```bash
    cd babyCode_simple
    ```

2.  **Install dependencies**:
    ```bash
    npm install
    ```

3.  **Run locally**:
    ```bash
    npm run dev
    ```

## 📂 Project Structure

- **`src/components`**: Holds the presentational components (`Hero.jsx`, `Features.jsx`, etc.).
- **`src/App.jsx`**: Arranges the components to form the landing page layout.