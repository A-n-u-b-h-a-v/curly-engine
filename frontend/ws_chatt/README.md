# WS Chatt - Real-Time Chat Application

A real-time chat application featuring a modern React frontend and a TypeScript backend.

## 🚀 Tech Stack

### Frontend
- **[React](https://react.dev/)**: Building the user interface.
- **[Vite](https://vitejs.dev/)**: Fast development server and build tool.
- **[Tailwind CSS](https://tailwindcss.com/)**: Utility-first styling.
- **[TypeScript](https://www.typescriptlang.org/)**: Type safety for client-side logic.

### Backend
- **[Node.js](https://nodejs.org/)**: Runtime environment.
- **[TypeScript](https://www.typescriptlang.org/)**: Type-safe server logic.
- **(Assumed)**: WebSockets (likely `ws` or `socket.io` given the project name) for real-time communication.

## 🛠️ Installation & Setup

1.  **Clone the repository**:
    ```bash
    git clone <repository-url>
    cd ws_chatt
    ```

2.  **Setup Backend**:
    ```bash
    cd backend
    npm install
    npm run dev
    ```

3.  **Setup Frontend**:
    ```bash
    cd frontend
    npm install
    npm run dev
    ```

## 📂 Project Structure

- **`backend/`**: TypeScript server handles chat connections.
- **`frontend/`**: React application provides the chat interface.
